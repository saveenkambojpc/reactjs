import { Typography } from "@mui/material";

export default function Logo() {
    return (
        <Typography color="primary" fontWeight={600} variant="h5" sx={{ flexGrow: 1 }}>
            Health App
        </Typography>
    )
}